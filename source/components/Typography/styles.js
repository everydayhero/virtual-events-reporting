import {
  rhythm,
  scale
} from '../../utils/css'

const heading = {}

export default {
  heading1: {
    ...heading,
    marginTop: rhythm(1.5),
    marginBottom: rhythm(1),
    fontSize: scale(2)
  }
}
